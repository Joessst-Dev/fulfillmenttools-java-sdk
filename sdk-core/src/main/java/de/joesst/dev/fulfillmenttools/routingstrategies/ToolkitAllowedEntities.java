package de.joesst.dev.fulfillmenttools.routingstrategies;

/**
 * The entities that can be compared by a toolkit fence or toolkit rating.
 */
public enum ToolkitAllowedEntities {
    /** Order entity. */
    ORDER,
    /** Facility entity. */
    FACILITY,
    /** Carrier connection entity. */
    CARRIERCONNECTION,
    /** Listing entity. */
    LISTING,
    /** Ordering facility entity. */
    ORDERING_FACILITY,
    /** Ordering facility groups entity. */
    ORDERING_FACILITY_GROUPS
}
